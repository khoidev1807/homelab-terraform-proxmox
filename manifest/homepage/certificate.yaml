apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: homepage-ui-certficate
  namespace: default
spec:
  secretName: homepage-ui-certificate-secret
  issuerRef:
    name: cloudflare-cluster-issuer
    kind: ClusterIssuer
  dnsNames:
    - homepage.nguyenvietkhoi.io.vn