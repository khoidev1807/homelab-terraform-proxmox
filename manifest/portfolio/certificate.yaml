apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: portfolio-certficate
  namespace: portfolio
spec:
  secretName: portfolio-certificate-secret
  issuerRef:
    name: cloudflare-cluster-issuer
    kind: ClusterIssuer
  dnsNames:
    - portfolio.nguyenvietkhoi.io.vn