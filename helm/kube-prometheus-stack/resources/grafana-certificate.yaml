apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: prometheus-certficate
  namespace: monitoring
spec:
  secretName: prometheus-certificate-secret
  issuerRef:
    name: cloudflare-cluster-issuer
    kind: ClusterIssuer
  dnsNames:
    - prometheus.nguyenvietkhoi.io.vn